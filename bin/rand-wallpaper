#!/bin/bash

wallpaperDir=$HOME/Pictures/wallpaper
    
function genRandWallpaper {
    local min=1
    local max=$(ls -1 $wallpaperDir | wc -l)
    echo $wallpaperDir/\
        $(\
        ls -1 $wallpaperDir | awk "NR==$(shuf -i $min-$max -n 1)" \
        )
}

genRandWallpaper
