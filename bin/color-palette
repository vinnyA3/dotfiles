#!/bin/bash

imageMagick() {
    special=''
    colors=''
    # create raw color palette(1) with imagemagick
    collect=( $( convert $1 -resize 25% +dither -colors 16 \
        -unique-colors txt:- | grep -oE '#.{6}' ) )
    printf '%s\n' "${collect[@]}"
}


imageMagick $1
