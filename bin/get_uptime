#!/bin/bash
# Unfortunately, I can't put this short script in a function...
# tmux doesn't play well with user-defined bash functions and
# that's where I'm using this really ...
uptime | awk '{print $3}' | sed 's/,$//'
